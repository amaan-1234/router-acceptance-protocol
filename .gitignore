# data + model outputs (regenerate via run/prepare_data, extract_embeddings, inference)
# leading slash anchors to repo root so `cac/data/` (a Python package) is NOT ignored
/data/
/outputs/
.hf_cache/

# python
__pycache__/
*.pyc
.venv*/
.pytest_cache/

# os/editor
.DS_Store
Thumbs.db
logs/
.ipynb_checkpoints/
*.tex
cac_aaai_*
# build artifacts / paper exports — keep source elsewhere (Overleaf)
paper_overleaf/
paper_overleaf.pdf
*.bak
*.bak[0-9]
